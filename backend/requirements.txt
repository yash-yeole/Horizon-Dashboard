fastapi>=0.115.6
uvicorn[standard]>=0.34.0
httpx>=0.28.1
pydantic>=2.11.0
pydantic-settings>=2.8.0
openpyxl>=3.1.5
websockets>=13.1
# Phase 7 paper-trading engine (light runtime deps only — the model/sklearn
# pipeline is invoked solely by the offline refresh_fairvalue.py runner).
pandas>=2.3.0
numpy>=2.2.0
pyarrow>=19.0.0
# FinBERT news sentiment (local CPU inference). The Dockerfile installs the
# CPU-only torch wheel explicitly (the default PyPI wheel pulls ~2GB of CUDA
# libs we don't need); torch is listed here so local dev installs it too —
# pip on Windows/macOS defaults to the CPU build.
torch>=2.2.0
transformers>=4.40.0
