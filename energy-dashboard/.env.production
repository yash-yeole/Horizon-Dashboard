# Production API base — the deployed HORIZON backend on Hugging Face Spaces.
# Vite inlines this into the build (`npm run build`), so the deployed frontend
# calls the backend Space instead of its own static origin.
# Change this if the backend Space is renamed/moved.
VITE_API_BASE_URL=https://yashyeole-horizon-backend.hf.space
