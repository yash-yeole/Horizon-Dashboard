server {
    listen 7860;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths fall back to index.html so client-side
    # routing (React Router) resolves instead of 404-ing.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
